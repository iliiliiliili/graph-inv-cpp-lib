// #include <osigma/ograph.hpp>
// #include <vector>

// using ograph::OGraph;

// // template <
// //     typename TId,
// //     typename TConnectionWeight,
// //     typename TCoordinates,
// //     typename TZIndex,
// //     typename TNodeFeatures,
// //     typename TConnectionFeatures>
// // OGraph<TId, TConnectionWeight, TCoordinates, TZIndex, TNodeFeatures, TConnectionFeatures>::OGraph(std::vector<TId> ids) : m_ids(ids) {}

// // template <typename TId, typename TConnectionWeight, typename TCoordinates, typename TZIndex, typename TNodeFeatures, typename TConnectionFeatures>
// // std::string ograph::OGraph<TId, TConnectionWeight, TCoordinates, TZIndex, TNodeFeatures, TConnectionFeatures>::describe() const
// // {
// //     return "OGraph(" + std::to_string(m_ids.size()) + " ids" + ")";
// // }

// template <typename TId>
// OGraph<TId>::OGraph(TId ids) : m_ids(ids) {}

// template <typename TId>
// std::string ograph::OGraph<TId>::describe() const
// {
//     return "OGraph(" + std::to_string(m_ids) + " ids" + ")";
// }



// template class OGraph<int>;